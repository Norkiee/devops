# Tasklist backend requires NO environment variables.
#
# Authentication is via a user-supplied Azure DevOps Personal Access Token (PAT),
# passed per-request from the plugin and forwarded to Azure as HTTP Basic auth.
# There is no OAuth app, no client secret, and no database.
